var token = artifacts.require("BCSToken");

contract('BCSToken', function(accounts) {
        
    // it("Token cap should be 1000", function() {
    //     return token.deployed().then(function(instance) {
    //         return instance.totalSupply.call();
    //     }).then(function(value) {
    //         assert.equal(value, 1000, "error, not 1000 cap");
    //     });
    // });


    //  it("test2", function() {
    //      console.log("TEST2 BEGINS");
    //      console.log(token.address);
    //      var inst = token.deployed();
    //      console.log(inst);
    //      var val = inst.totalSupply.call();
    //      assert.equal(val, 1000, "!!!");
    //  });

});
